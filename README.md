# Temperature blanket plotter

## Getting started

1. Create virtual env
```bash
python3 -m venv .venv
source .venv/bin/activate
```

2. Install libraries
```bash
pip install -r requirements.txt
```

3. Run the notebook
